TASK 1:  annual gross salary of £24,000.00


// Given
SalarySlip salaryresult = new SalarySlip();  

// When
SalarySlip salaryresult = salarySlipTest.getSalarySlip(24000);

// Then
assertNotNull(salaryresult);
assertTrue(salaryresult.getValue.equals(216.67));
assertNull(salaryresult...);



TASK 3: annual salary = 45 000


TASK 3: annual salary > 100 000
- AssertNationalInsuranceContributionsFor(101000, ??)
- AssetTaxableIncome(???, ????)

// Given
SalarySlip salaryresult = new SalarySlip();

// When
SalarySlip salaryresult = salarySlipTest.getSalarySlip(101000);

// Then
assertNotNull(salaryresult);
assertTrue(salaryresult.getSalarySlip.getTAxPayable().equals(xxx));
assertNull(salaryresult...);


CODE DRAFT
public SalarySlip generateFor(Employee employeeWithSalary24000) {

        double maximumSalary = 100 000;
        double taxPercentageWhenUnder100k = 20;  // TODO: remove percentage useless

        SalarySlip response = new SalarySlip();
        SalarySlip.setTaxPayable(true);

        if (salary < maximumSalary) {
            response.setValue(salary - (salary * taxPercentageWhenUnder100k) );
            return response;
        }
        if (salary >  maximumSalary) {
            //response.setValue(salary - (salary * taxPercentageWhenUnder100k) );
            return response;
        }

        public void getSalarySlip(double salary) {

            // special case over 100 000
            if (salary > 100000) {

                double salary

            } else {

            }

        }

